See [AGENTS.md](AGENTS.md) — the inventory of what this project already
implements, the stack constraints, and the bugs not to reintroduce.

Kept as a pointer rather than a copy so the two cannot drift apart.
